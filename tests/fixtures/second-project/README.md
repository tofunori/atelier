# Second project fixture

Independent project used by daemon multi-project isolation tests.
Deliberately reuses common filenames (`tiny.png`, `script.py`, `notes.md`)
so isolation failures are obvious.
